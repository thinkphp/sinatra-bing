# A Simple Search Interface for Microsoft's Bing using Sinatra.

## Install

```
$ git clone git://github.com/thinkphp/sinatra-bing.git
$ cd sinatra-bing
$ bundle install #generate Gemfile.lock
$ rackup

```